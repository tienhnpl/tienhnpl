# JDBC URL k?t n?i MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/salary_management_system?useSSL=false&serverTimezone=UTC

spring.datasource.username="root"
spring.datasource.password=""

# Hibernate s? t? t?o b?ng n?u ch?a có
spring.jpa.hibernate.ddl-auto=update

# Dialect MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# T?t cache thymeleaf
spring.thymeleaf.cache=false
